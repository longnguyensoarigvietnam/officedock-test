version: '3.8'

networks:
  office_dock_network:
    external: true

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: officedock-system
    ports:
      - '3000:3000'
    env_file:
      - .env
    networks:
      - office_dock_network
